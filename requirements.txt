# =============================================================================
# Agentic Controller Designer — Python dependencies
# Install:
#   pip install -e "packages/labcd_agents[all]"    # shared LLM foundation
#   pip install -e "packages/labcd_pdfmaker"       # shared PDF report builder
#   pip install -r requirements.txt
# =============================================================================

# --- Shared / AgentAdaptive ---
langchain
langchain-openai
langchain-core
sympy
numpy
matplotlib
pydantic
streamlit
pyyaml
# HTTP stack for langchain-openai (explicit: partial installs surface as
# ModuleNotFoundError inside ChatOpenAI rather than at import time)
httpx
httpcore

# Shared LLM-agent foundation: labcd_agents.LLMFactory, tokens, prompts.
# Prefer editable install from the vendored package (supports provider extras):
#   pip install -e "packages/labcd_agents[all]"
# The bare name below is a fallback if the package is published/indexed.
labcd_agents

# Shared PDF report builder, currently used by AgentAdaptive (reportlab
# backend always available; xelatex backend needs a system LaTeX install,
# see packages.txt). Prefer editable install:
#   pip install -e "packages/labcd_pdfmaker"
labcd_pdfmaker

# --- AgentMPC core (dynamics + mpc; no LLM required) ---
scipy>=1.10
osqp>=0.6.3

# --- AgentMPC agents / graph ---
langgraph>=0.1
python-dotenv

# --- AgentMPC Streamlit UI ---
# streamlit is listed above; extras for reports / tables / images:
reportlab>=4.0
pillow>=9.0.0
pandas>=2.0

# --- AgentPlant FastAPI layer ---
fastapi>=0.110
uvicorn[standard]>=0.27

# --- Dev / tests ---
pytest>=7.0
httpx>=0.27

# --- Optional (uncomment as needed) ---
# torch>=2.0          # AgentMPC analytic Jacobians (else finite differences)
# langchain-groq      # usually pulled via labcd_agents[all]
# langchain-anthropic
